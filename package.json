{
  "name": "ee_api_demo",
  "private": true,
  "version": "0.0.0",
  "description": "Demo page featuring Editoreye API demo content",
  "repository": "https://github.com/stuartwilsondev/ee_api_demo",
  "license": "None",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8008 -c-1",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  }
}
